import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  appBar: {
    display: 'flex',
    boxShadow: 'none',
  },
  heading: {
    color: 'rgba(0,183,255, 1)',
  },
}));
